node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# 환경 변수 파일 (비밀 정보 포함)
.env
.env.*
.env.local
.env.*.local
.env.backup

# HTTPS 인증서 파일 (로컬 개발용)
*.pem

# 엑셀 파일 (직원 데이터 - 민감 정보)
data/employees.xlsx